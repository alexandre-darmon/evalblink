[pytest]
# Add the repo root to sys.path so `from evalblink import ...` works without
# installing the package — CI runs bare `pytest` in a fresh env where evalblink
# is not pip-installed.
pythonpath = .
testpaths = tests
